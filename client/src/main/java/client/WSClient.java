package client;

import client.websocket.NotificationHandler;
import websocket.messages.ServerMessage;

public class WSClient implements NotificationHandler {
    public void notify(ServerMessage serverMessage) {

    }
}
